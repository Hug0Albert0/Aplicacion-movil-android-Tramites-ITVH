<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hug0albert0.vistas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".validacion" />
        <activity android:name=".credenciales" />
        <activity android:name=".recuperar" />
        <activity android:name=".tramitesAgendados" />
        <activity android:name=".detallesCita" />
        <activity android:name=".problemas" />
        <activity android:name=".serviceFeedback" />
        <activity android:name=".avisos" />
        <activity android:name=".catalogoTramites" />
        <activity android:name=".informacionTramite"></activity>
    </application>

</manifest>